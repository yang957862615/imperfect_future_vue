// const isProdMode = Object.is(process.env.NODE_ENV, 'production');

/*const baseURL = {
    apiBaseURL: isProdMode ? 'https://api.joegreens.cn/' : 'http://localhost:8001/'
};*/
const baseURL = {
    apiBaseURL: 'http://localhost:8001/'
};

export default baseURL;